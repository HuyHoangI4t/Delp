program CSDLtin23;

uses
  Vcl.Forms,
  UMain in 'UMain.pas' {CSDL};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  AApplication.CreateForm(TCSDL, CSDL);
  pplication.Run;
end.
